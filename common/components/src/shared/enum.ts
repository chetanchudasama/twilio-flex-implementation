export enum CustomToggleState {
  YES,
  NO,
  INTERMEDIATE,
}

export enum FileType {
  Image = 1,
  Audio = 2,
  Other = 3,
}

export enum ApplicationStatusName {
  Qualify = "Qualify",
  CarSearch = "Car Search",
  Quotes = "Quotes",
  DealSheet = "Deal Sheet",
  Deal = "Deal",
  ValCall = "Val Call",
}

export enum ApplicationStatusId {
  Qualify = 1,
  CarSearch = 2,
  Quotes = 3,
  DealSheet = 4,
  Deal = 5,
  ValCall = 6,
}

export enum ThirdPartyRemovalAuthor {
  Customer = 1,
  Broker = 2,
}

export enum VulnerableCustomerStatus {
  suspected = 1,
  confirmed = 2,
}

export enum CustomerDetailType {
  CustomerDetail = 1,
  AddressHistory = 2,
  EmploymentHistory = 3,
  BankDetails = 4,
}
