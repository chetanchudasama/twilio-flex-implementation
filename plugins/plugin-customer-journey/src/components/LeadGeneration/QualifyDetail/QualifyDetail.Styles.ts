import { black } from "@common/components";
import styled from "react-emotion";

export const QualifyDetailStyles = styled("div")({
  padding: 24,
  marginLeft: -8,
  ".qualify-detail-heading": {
    fontSize: 20,
    lineHeight: "24px",
    fontWeight: 600,
    color: black,
    marginBottom: 4,
  },
});
